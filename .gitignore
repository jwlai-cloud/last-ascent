node_modules/
.DS_Store

# packaging output — rebuilt by `npm run package`, never hand-edited
dist/
last-ascent.zip

.firecrawl/

# screenshots the suite and probes rewrite on every run
tests/*.png

# Scraped copies of the official docs live in docs/source/ as committed text.
# The raw PDFs and scrape cache do not need to ship.
.firecrawl/
